# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local environment (keep example committed)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Cursor local machine config (keep .cursor/rules/** shareable / reviewable)
.cursor/mcp.json
/.cursor/rules.md

# Local benchmark outputs (may contain timing/token metrics)
tmp/

# Supabase CLI local cache (versions, linked project metadata)
supabase/.temp/
