/**
 * Legacy fragment architecture removed — Product Story is now an iPad theater.
 * File retained empty so stale imports fail loudly at typecheck if any remain.
 */
export const PRODUCT_STORY_FRAGMENTS = [] as const
