/** Compact phone product tour timeline constants (3F.1) — no UI imports. */

/** Calm beat after phone lands, before tour. */
export const COMPACT_PHONE_TOUR_SETTLE_DELAY_MS = 500

/**
 * Total autonomous tour duration — maps desktop appProgress 0→1.
 * Holds are proportionally preserved via linear remap.
 */
export const COMPACT_PHONE_TOUR_DURATION_S = 11
