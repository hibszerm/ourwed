/**
 * Neutral shared Assistant infrastructure used by Golden V7.
 * Deterministic CRM adapters, registry, and universes — not a legacy runtime.
 */

export * from './registry'
export type { ConceptInspectValue, ConceptScalarValue } from './adapters/types'
