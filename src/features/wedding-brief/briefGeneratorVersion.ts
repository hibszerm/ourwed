/**
 * Brief HTML/layout/template version.
 *
 * Increment when a change to the Brief generator, registry destinations,
 * HTML/CSS template, or included fields would produce a different PDF
 * from the same canonical source data.
 *
 * Do not increment for fingerprint/canonicalization-only changes that
 * do not alter rendered PDF output.
 *
 * Stored separately from source_hash so tests can distinguish:
 * DATA changed vs GENERATOR changed.
 */
export const CURRENT_BRIEF_GENERATOR_VERSION = 1
