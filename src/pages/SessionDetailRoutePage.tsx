import { SessionDetailModernPage } from '@/pages/SessionDetailModernPage'

/**
 * Product `/sesje/:sessionId` route — Modern only.
 */
export function SessionDetailRoutePage() {
  return <SessionDetailModernPage />
}
