import { WeddingDetailModernPage } from '@/pages/WeddingDetailModernPage'

/**
 * Product `/sluby/:id` route — Modern workspace only.
 */
export function WeddingDetailRoutePage() {
  return <WeddingDetailModernPage />
}
